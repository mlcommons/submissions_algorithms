name: Validate PR Template

on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Validate YAML in PR Body
        uses: actions/github-script@v6
        with:
          script: |
            const prBody = context.payload.pull_request.body || '';
            // ... (Insert your YAML extraction & validation logic here) ...
            
            if (emptyFields.length > 0) {
              core.setFailed(`Missing fields: ${emptyFields.join(', ')}`);
              // Note: This will fail the check, but won't be able to comment 
              // on a fork PR. That's okay; the user sees the Red X.
            }


name: Process Submission Template

on:
  pull_request_target:
    types: [opened, edited, synchronize]

jobs:
  check_and_sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout Base (Your Repo)
        uses: actions/checkout@v3
        with:
          # We checkout your repo to ensure the 'push' goes to the right place
          fetch-depth: 0

      - name: Process Template
        uses: actions/github-script@v6
        id: processor
        with:
          script: |
            const yaml = require('js-yaml'); 
            // Note: You may need to pre-install js-yaml in a previous step 
            // or use a different parser available in the environment.
            
            const pr = context.payload.pull_request;
            const body = pr.body;
            
            // 1. VALIDATION LOGIC
            // ... (Your extraction logic here) ...
            
            // 2. OUTPUT DATA FOR NEXT STEP
            core.setOutput('is_valid', true);
            core.setOutput('yaml_data', JSON.stringify(data));
            core.setOutput('path', submissionPath);

      - name: Commit Submission File
        if: steps.processor.outputs.is_valid == 'true'
        run: |
          # Use the data from the script to create the file
          mkdir -p "${{ steps.processor.outputs.path }}"
          echo '${{ steps.processor.outputs.yaml_data }}' > "${{ steps.processor.outputs.path }}/submission_info.yml"
          
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "Auto-sync submission for PR #${{ github.event.pull_request.number }}"
          git push